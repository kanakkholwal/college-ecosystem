// TODO: Adapt auth client for SvelteKit (the original imports from ~/auth/client which does not exist yet)
// export type { Session } from "$lib/auth/client";

// Placeholder: re-export once $lib/auth/client is created
export type Session = Record<string, unknown>;
